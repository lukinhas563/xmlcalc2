type Styles = 'header'

const style: Record<Styles, React.CSSProperties> = {
    header: {
        height: '30vh',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    },
}

export default style
